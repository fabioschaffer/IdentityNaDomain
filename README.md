# IdentityNaDomain

Projeto utilizando Identity. A entidade ApplicationUser herda de IdentityUser. Para que isso funcione, foi necessário adicionar referencia do pacote Microsoft.AspNetCore.Identity.EntityFrameworkCore no projeto Domain. Por isso o nome da solution é Identity na Domain.
